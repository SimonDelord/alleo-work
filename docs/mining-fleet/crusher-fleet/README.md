# Crusher fleet

Fixed plant (`crusher-fleet`) — wired automation with Modbus TCP PLCs, a local plant historian, and batch CSV export to object storage.

Detailed design docs will follow: OpenShift namespace layout, Modbus register maps, plant-collector and historian-export services, and S3 export patterns.
