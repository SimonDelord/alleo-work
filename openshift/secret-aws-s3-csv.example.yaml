# Copy to a local file, replace placeholders, apply with:
#   oc apply -f secret-aws-s3-csv.local.yaml
# Or use: oc create secret generic ... --from-literal=...
# Do NOT commit real keys to Git.
apiVersion: v1
kind: Secret
metadata:
  name: aws-s3-csv-creds
  namespace: kafka-demo
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "REPLACE_AKIA"
  AWS_SECRET_ACCESS_KEY: "REPLACE_SECRET"
